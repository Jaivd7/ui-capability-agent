{
  "apps": {
    "legacy-core-banking": {
      "allowedOrigins": ["http://localhost:4000"],
      "allowedRoutePatterns": ["^/(login|logout|members(/.*)?|dev/expire-session)$"],
      "allowedActionTypes": ["navigate", "click", "fill", "select", "check", "waitFor", "extract"],
      "irreversibleActionPolicy": "block"
    },
    "meridian-core": {
      "_note": "/settings is deliberately NOT allowlisted. It is the target's global fault-injection config; a capability replay must never be able to reach it. The dashboard's fault panel drives it out of band, with its own session, outside the replay path.",
      "allowedOrigins": ["https://web-sample.interface-hiring.com"],
      "allowedRoutePatterns": ["^/(signon|signoff|menu|members(/.*)?)$"],
      "allowedActionTypes": ["navigate", "click", "fill", "select", "check", "waitFor", "extract"],
      "irreversibleActionPolicy": "block"
    }
  }
}
