{
  "capabilityId": "lookup-member-balance",
  "score": 34,
  "grade": "D",
  "metrics": {
    "steps": 4,
    "chains": 7,
    "meanChainDepth": 1,
    "singleCandidateChains": 7,
    "brittleRootedChains": 0,
    "brittleOnlyChains": 0,
    "checkpoints": 3,
    "structuralCheckpoints": 2,
    "paramsDeclared": 1,
    "paramsBoundInSteps": 1,
    "paramsAssertedInCheckpoints": 1,
    "outputs": 1,
    "outputsVerified": 1
  },
  "findings": [
    {
      "severity": "warn",
      "code": "single_candidate_chain",
      "where": "steps[0].locator",
      "message": "no fallback candidate; the ordered chain degrades to a single selector",
      "suggestion": "record 2-3 candidates, or state in the reason why no fallback exists"
    },
    {
      "severity": "warn",
      "code": "single_candidate_chain",
      "where": "steps[1].locator",
      "message": "no fallback candidate; the ordered chain degrades to a single selector",
      "suggestion": "record 2-3 candidates, or state in the reason why no fallback exists"
    },
    {
      "severity": "info",
      "code": "weak_reason",
      "where": "steps[1].locator[0].reason",
      "message": "reason is too short to tell a reviewer why this locator was trusted"
    },
    {
      "severity": "warn",
      "code": "single_candidate_chain",
      "where": "steps[2].locator",
      "message": "no fallback candidate; the ordered chain degrades to a single selector",
      "suggestion": "record 2-3 candidates, or state in the reason why no fallback exists"
    },
    {
      "severity": "warn",
      "code": "single_candidate_chain",
      "where": "steps[3].locator",
      "message": "no fallback candidate; the ordered chain degrades to a single selector",
      "suggestion": "record 2-3 candidates, or state in the reason why no fallback exists"
    },
    {
      "severity": "warn",
      "code": "single_candidate_chain",
      "where": "checkpoints[0].locator",
      "message": "no fallback candidate; the ordered chain degrades to a single selector",
      "suggestion": "record 2-3 candidates, or state in the reason why no fallback exists"
    },
    {
      "severity": "warn",
      "code": "single_candidate_chain",
      "where": "checkpoints[1].locator",
      "message": "no fallback candidate; the ordered chain degrades to a single selector",
      "suggestion": "record 2-3 candidates, or state in the reason why no fallback exists"
    },
    {
      "severity": "warn",
      "code": "single_candidate_chain",
      "where": "checkpoints[2].locator",
      "message": "no fallback candidate; the ordered chain degrades to a single selector",
      "suggestion": "record 2-3 candidates, or state in the reason why no fallback exists"
    },
    {
      "severity": "error",
      "code": "differential_probe_failed",
      "where": "(differential probe)",
      "message": "replaying with {\"memberId\":\"1002\"} gave hard_failure — Member heading shows the Member: prefix confirming correct record opened: Checkpoint not met: Member heading shows the Member: prefix confirming correct record opened",
      "suggestion": "this flow passed live moments ago with the recorded arguments, so something in it is fitted to those arguments rather than to the app"
    }
  ]
}